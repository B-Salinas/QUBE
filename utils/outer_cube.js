let outer_cube = {
  width: 1.7,
  height: 1.7,
  depth: 1.7,
  material: {
    color: 0xfdfefe, // white
    wireframe: true,
  },
};

// these require Three.JS, which would be something that I have to import anyways ... maybe not doing this here
let outer_geometry = {};

let outer_material = {};
